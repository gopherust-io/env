// Code generated by envgen. DO NOT EDIT.

package bench

import (
	env "github.com/gopherust-io/env"
)

func LoadSmallConfig() (SmallConfig, error) {
	snap := env.Snapshot()
	var cfg SmallConfig
	if err := loadSmallConfig(&cfg, snap); err != nil {
		return cfg, err
	}
	return cfg, nil
}

func MustLoadSmallConfig() SmallConfig {
	cfg, err := LoadSmallConfig()
	if err != nil {
		panic(err)
	}
	return cfg
}

func (c SmallConfig) Masked() SmallConfig {
	out := c
	out.AdminPassword = env.SensitiveMask
	return out
}

func loadSmallConfig(cfg *SmallConfig, snap *env.EnvSnapshot) error {
	var errs []env.FieldError
	{
		key := "R9645Z7I"
		raw, ok := snap.Lookup(key)
		if !ok || raw == "" {
			cfg.HTTPAddr = ":8080"
		} else {
			cfg.HTTPAddr = raw
		}
	}
	{
		key := "UHBLDE6J"
		raw, ok := snap.Lookup(key)
		if !ok || raw == "" {
			cfg.NATSURL = "nats://localhost:4222"
		} else {
			cfg.NATSURL = raw
		}
	}
	{
		key := "IF9U8G21"
		raw, ok := snap.Lookup(key)
		if !ok || raw == "" {
		} else {
			cfg.NATSCredsFile = raw
		}
	}
	{
		key := "QROT5LSO"
		raw, ok := snap.Lookup(key)
		if !ok || raw == "" {
		} else {
			cfg.NATSToken = raw
		}
	}
	{
		key := "5SRWSTAX"
		raw, ok := snap.Lookup(key)
		if !ok || raw == "" {
			cfg.MonitoringURL = "http://localhost:8222"
		} else {
			cfg.MonitoringURL = raw
		}
	}
	{
		key := "XH6IIE4A"
		raw, ok := snap.Lookup(key)
		if !ok || raw == "" {
			v, err := env.ParseDuration("10s")
			if err != nil {
				env.AppendParse(&errs, "RequestTimeout", key, "10s", err)
			} else {
				cfg.RequestTimeout = v
			}
		} else {
			v, err := env.ParseDuration(raw)
			if err != nil {
				env.AppendParse(&errs, "RequestTimeout", key, raw, err)
			} else {
				cfg.RequestTimeout = v
			}
		}
	}
	{
		key := "7P8ERDIA"
		raw, ok := snap.Lookup(key)
		if !ok || raw == "" {
		} else {
			cfg.StaticDir = raw
		}
	}
	{
		key := "S74XQGDI"
		raw, ok := snap.Lookup(key)
		if !ok || raw == "" {
			cfg.AdminUsername = "admin"
		} else {
			cfg.AdminUsername = raw
		}
	}
	{
		key := "1T6U258X"
		raw, ok := snap.Lookup(key)
		if !ok || raw == "" {
			cfg.AdminPassword = "admin"
		} else {
			cfg.AdminPassword = raw
		}
	}
	{
		key := "412E3U10"
		raw, ok := snap.Lookup(key)
		if !ok || raw == "" {
			v, err := env.ParseBool("true")
			if err != nil {
				env.AppendParse(&errs, "AuthEnabled", key, "true", err)
			} else {
				cfg.AuthEnabled = v
			}
		} else {
			v, err := env.ParseBool(raw)
			if err != nil {
				env.AppendParse(&errs, "AuthEnabled", key, raw, err)
			} else {
				cfg.AuthEnabled = v
			}
		}
	}
	return env.NewError(errs)
}
